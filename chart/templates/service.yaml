apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
  - port: 5000
    protocol: TCP
    name: web
  selector:
    app: {{ .Values.name }}

